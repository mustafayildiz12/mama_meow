import 'package:flutter/widgets.dart';

class PushNotiication extends StatelessWidget {
  const PushNotiication({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}